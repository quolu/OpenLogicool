学習した操作画面へ教師付き開始／次の一手／停止を配線し、現在操作・期待前後画面・監査・Nano送信状態・停止理由・全履歴を日本語投影。保存／元に戻す右下配置を維持。通常fast pathとの同時稼働を禁止し、非resident OpenLogicool.Host uiだけがNano専用runtimeを所有、resident UIは専用mode再起動を明示。Desktop presenter 2件・Host実runtime 11件green。temp実SQLiteへNIKKE packageをimportし、Windows native実画面で2 stepと有効な開始buttonを目視、入力dispatch 0。