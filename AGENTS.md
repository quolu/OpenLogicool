# OpenLogicool — プロジェクト正典

Logicool G13 / G600 を統合する Windows ネイティブアプリ。LGS 9.04.49 の代替となる **Input Studio**（アプリ先行の両デバイス統合設定）と、画面認識付き逐次学習プレイブック基盤 **Game Operator** を、同じ意味操作・アプリプロファイルの上で提供する。

## 正本の所在

- 製品・開発計画の正本: [docs/development-plan.md](docs/development-plan.md)（要件catalog・architecture契約・Phase計画・release gateはこの一冊だけが正）
- 調査知識: [rag/INDEX.md](rag/INDEX.md)（成立性調査・一次資料台帳）
- 障害記録: docs/incidents/
- 本書は計画を複製しない。計画と矛盾したら計画側が正であり、本書を修正する。

## 現在の状態（2026-08-19）

- **Phase 1 完了（Exit 4条件成立）**。判定は [docs/phase1-exit-assessment.md](docs/phase1-exit-assessment.md)。次は Phase 2 だが、**G0-Device-W（実機 write 実験）を他実装より先に単独実施**する順序であり、実機手番とオーナー裁定を要する。
- **G0-Device-RO 通過（オーナー裁定 2026-08-15）**: G13 入力経路・G600 profile read（F6 のみ read 不能を記録）・G600 live input route（全20 control）・Migration Safety Gate 手順定義（[docs/migration-safety-gate.md](docs/migration-safety-gate.md)）・方式 read-only 判定（[docs/g600-route-assessment-2026-08-15.md](docs/g600-route-assessment-2026-08-15.md)）の5条件成立。
- **G0-Automation 通過（オーナー裁定 2026-08-15）**: 実frame連鎖・Data Flow Contract 項目決定・NIKKE 混同なしの3条件成立（[docs/g0-automation-assessment.md](docs/g0-automation-assessment.md)）。
- **G0-Device-W: onboard write 往復成立（2026-08-15 夜・オーナー裁定）**。初回セッションで feature write のずれ（single write・settle なし・handle 再利用が原因）→ マウス操作不能の二次障害を出したが、公開実装調査で「direct SET_FEATURE は正規経路、真因は firmware の settle/handle 敏感性」を解明（[rag/openlogicool/g600-write-protocol-2026-08-15.md](rag/openlogicool/g600-write-protocol-2026-08-15.md)）。evidence-based 作法（fresh open・settle 2s・handle 非再利用・fresh open で verify・一致まで再送）を実装し、**restore（F3/F5 復元）と apply 往復（意図的改変→byte 一致→restore→byte 一致）を実機で成立**（いずれも attempt1 で一致）。詳細は [docs/incidents/2026-08-15-g600-f3-writeback-shift.md](docs/incidents/2026-08-15-g600-f3-writeback-shift.md)。これにより Migration Safety Gate DEV-010 の一巡が実証済み。write 作法は probe の `g600-restore-retry` / `g600-apply-verify` を正とする。F0〜F5 完全 backup は `probe-output/mig01-backup-20260815/` に SHA-256 封入で保持。**0A UI 照合は完了**（[docs/lgs-parity-inventory-2026-08-15.md](docs/lgs-parity-inventory-2026-08-15.md) §5——XML 一次資料判定を維持、UI 限定機能6面と G13 onboard 転送面を記録）。**EXP-G600-03（F0 slot 切替）も成立**（slot 0→1→0 往復・F0 表現モデル実測確定・`probe-output/g600-slot-cycle-20260815-130235-294.json`）。**G0-Device-W は全項目実機実証済み**: restore・apply 往復・slot 切替（方式A成立）に加え、**EXP-G600-02 write 拡張も成立**（方式B変種: G9→F13 書換えで legacy 配送が中間 usage へ変化・raw route 無影響・LGS 巻き戻しなし、[docs/g600-route-assessment-2026-08-15.md](docs/g600-route-assessment-2026-08-15.md) §5）。**G0-Device-W 通過（オーナー裁定 2026-08-15）**: 実証全項目成立＋**route 最終決定「B変種を主経路・Aを補完・C不採用」**（[docs/g600-route-assessment-2026-08-15.md](docs/g600-route-assessment-2026-08-15.md) §5、計画 §16 に記録）。**Phase 2 着手済み**: EXP-IN-03（watchdog 実測）完了——SendInput 合成 key-down は hard kill 後も残留（OS 自動 release なし・2試行再現）、別 process key-up で release 成立。**watchdog 採用必須が確定**（計画 §16。昇格/uiAccess 採否のみ EXP-IN-01 elevated 実測待ち）。probe は `crash-keystate`。**G13 adapter 実装済み（2026-08-15 深夜）**: `Devices.G13` に pure parser（実測台帳 [docs/probes/g13-input-map-2026-08-15.md](docs/probes/g13-input-map-2026-08-15.md) の確認済み／強い推定 bit のみ・未確認 bit と jitter bit は無視）＋ recorded／live adapter（同一 `G13ReportStream` 経路）。実録 fixture（`fixtures/devices/g13-input-report.recheck.v1.json`・243 report）の replay が期待 edge 列12件と一致、live 実機 smoke（`g13-adapter-smoke`）で列挙・単独押下・chord・stick 押込み・stick sample 315件・drop 0 を確認（`probe-output/g13-adapter-smoke-20260815-234242.json`）。**G600 input adapter 実装済み（2026-08-16 未明）**: `Devices.G600` に pure parser（実測台帳 [docs/probes/g600-input-map-2026-08-15.md](docs/probes/g600-input-map-2026-08-15.md)。bit は G 番号と連番一致・byte4 ホイールは「最後の回転差分を latch」を実測で精密化——回転 event はボタン変化なし report だけ）＋ recorded／live adapter（同一 `G600ReportStream` 経路・page 0xFF80）。実録 fixture 2件（`fixtures/devices/g600-input-report.{normal,g9-g20}.v1.json`・全20 control とホイール両方向・latch 罠ケースを網羅）の replay が期待 edge 列48件＋26件・wheel tick 15件と一致（focused test 28件 green）。**live 実機 smoke（`g600-adapter-smoke`）成立（2026-08-16 朝）**: 列挙（実 ContainerId 取得）・サイド押下（G12/G16/G17）・G7/G8・G6(G-Shift)・wheel tick 上9/下12・全 edge 対・drop 0（`probe-output/g600-adapter-smoke-20260816-085722.json`）。**G600 side remap write モジュール実装済み（2026-08-16 朝）**: `Devices.G600` の `G600SideRemap`（pure）が clean F3 から side 12ボタン（G9〜G20）を中間 usage F13〜F24（0x68〜0x73）へ書き換えた payload を構築——**通常層（offset 55〜90）と G-Shift 層（118〜153）の両方**を書き換え（G6 押下中の shifted legacy 配送も無害化対象のため）、他 byte は bytes ごと保持。focused test 16件 green（offset・usage・保持性・非破壊・IsApplied・検証拒否）。probe `g600-side-remap`（clean 前提→apply→fresh verify→restore→fresh verify の往復）が**実機成立**: apply・restore とも attempt1 で byte 一致（`probe-output/g600-side-remap-20260816-000320-991.json`）。**残置運用は製品経路へ組込済み（2026-08-19）**: `G600LeftoverSession` が G600 配線時に F3 を SideRemap して残し、handled shutdown で baseline へ戻す。foreground 切替では書かない（MAP-010）。LGS / G HUB / Options+ 実行中は書かない。CLI `leftover apply|restore|status`。focused test 16件（policy／evidence write／session）。**実機確認はオーナー手番**（手順は [docs/g600-leftover-operation.md](docs/g600-leftover-operation.md)）。**Mapping Runtime 中核実装済み（2026-08-16 朝）**: `OpenLogicool.Input` に `MappingProfile`（pure data＋検証——layer 集合・latch selector＝G13 M1/M2/M3 相当・hold selector＝G600 G-Shift 相当・(control×layer)→output token binding）と `DeviceMappingRuntime`（device instance 単位の pure 状態機。既存 `PressOwnershipState` の上で MAP-003/005/006・DEV-007/008 を実装——down 時に profile/layer/mapping revision/output 集合を固定、up は再解決せず down 時集合だけ解放、layer/profile 変更は新規 down から有効、Stop で新規 down 抑止＋全所有 release）。I/O なし・ホイール tick は binding 対象外（button edge のみ）。focused test 18件 green（`tests/OpenLogicool.Input.Tests`）＋ architecture test 4件 green。**Input Emitter＋watchdog 実装済み・実測成立（2026-08-16 朝）**: `OpenLogicool.Input` に output token 解釈（`Key:<名前>`／`Vk:0xNN`／`Mouse:<button>`・pure・未知 token は拒否）、`SendInputEmitter`（chord は単一 SendInput call・部分成功は `OutputEmitFaultException` で fault 化し自動再送しない）、`WatchdogChannel`＋`GuardedOutputEmitter`（down は watchdog 通知→送出、up は送出→通知の順で「watchdog が知らない down」を構造的に排除）。`OpenLogicool.Watchdog` は依存ゼロの独立 exe（stdin 行 protocol・EOF＝host 死亡で追跡中 output を全 release・EXIT で release なし終了）。probe `emitter-smoke` が実測成立: roundtrip 8 check 全 pass（単一 key・chord・handled stop release・graceful exit）、**hard kill 後 35ms で watchdog release（NFR-008 250ms 予算内）**——DEV-009 の watchdog 条件を製品モジュールで実証（`probe-output/emitter-smoke-20260816-003043-049.json`）。UIPI/elevated 条件（EXP-IN-01）と昇格/uiAccess 採否は未決のまま。focused test: Input 48件 green・architecture 4件 green。**FastPathPump 実装済み（2026-08-16 朝）**: `OpenLogicool.Input` の `FastPathPump` が Device Input→Mapping Runtime→Input Emitter を専用 worker thread で接続（§6.1〜6.2 準拠——非 blocking pull・AI/network/capture/SQLite/UI を待たない・source の drop 検出/未知 device/emitter fault は継続せず fault 停止して全所有 output を release）。focused test 6件（計 Input 54件 green・同期 `RunOnce` ＋ worker thread 実走・fault 時 release 検証）。probe `fastpath-smoke` 実装済み（実機 G13/G600 →F13〜F24 送出・layer 動作・drop/fault/全 emit を JSON 証跡化）。**live 通し smoke 成立（2026-08-16 朝・実機）**: G13/G600 両実機を同時列挙し、45秒間で 30 input 処理・9 押下対（F14/F16/F19/F21〜F24）を down/up 完全対で送出・**drop 0・fault なし**——通常層・G600 G-Shift hold 層・G13 M2 latch 層の全動作を含む（`probe-output/fastpath-smoke-20260816-004442-598.json`）。**fast path（Device Input→Mapping Runtime→Input Emitter＋watchdog）は両実機で end-to-end 実証済み**。Phase 2 の残り実機項目: EXP-IN-01（standard/elevated 受理分類。elevated は UAC 承認要＝オーナー手番）。**Profiles 永続化実装済み（2026-08-16 朝）**: `MappingProfile` を計画 §6.3 どおり Domain へ移設（Input の runtime は Domain 参照のまま無変更）。`Contracts/Profiles` に wire type `MappingProfileDocument`＋port `IMappingProfileStore`、`Profiles` に materializer（document→MappingProfile。検証は Domain 構築子・未知 schema version 拒否）、`Persistence` に migration 002＋`SqliteMappingProfileStore`（versioned JSON 格納・再 open 復元・未知 version は読み飛ばさず fail）。focused test: Profiles 4件・Persistence 8件 green（temp file 再 open の復元実証込み）。**Host resident 化（最小形）実装済み・実機 smoke 成立（2026-08-16 朝）**: `OpenLogicool.Host` が exe 化——`ResidentInputHost`（SQLite open→migrate→profile 復元→実機列挙→fast path＋watchdog 起動→handled shutdown）、`SingleInstanceGuard`（named mutex）、`HostProfileSelection`（pure・DeviceKind ごと 0/1 profile、複数は明示エラー）、CLI `import <json>`／`run [--duration-ms]`。profile が無い種別は配線しない（黙って既定値を作らない）。**実機 smoke 成立**: import 2 profile→再起動復元→G13/G600 両実機 wired 2→resident→handled shutdown exit 0、二重起動は2プロセス実測で拒否（OPS-002 の設定復元と §6.2 の二重起動防止を実機で実証）。focused test: Host 4件 green。tray icon・既存 instance への UI activation 転送は Desktop UI 実装時。**EXP-IN-01 standard 側実測完了（2026-08-16 朝）**: probe `sendinput-accept`（test target window の受信 log と送信列の突合・API 戻り値に依らない判定）で、**standard foreground window への keyboard SendInput 配送は確認済み**——単一 key・chord とも6/6 完全順序一致（`probe-output/sendinput-accept-standard-20260816-014738-596.json`、計画 §16 に記録）。**EXP-IN-01 elevated 側実測完了（2026-08-16 昼・オーナー UAC 承認）**: **Blocked 確定**——target の昇格を TokenElevation で実測・foreground 確認済みの条件で、送信6 event の受信ゼロ（UIPI 遮断・`probe-output/sendinput-accept-elevated-20260816-022736-103.json`、計画 §16 に記録）。**EXP-IN-01 は standard＝Delivered／elevated＝Blocked で分類完了**。**watchdog 昇格/uiAccess 裁定済み（2026-08-16 オーナー裁定）**: uiAccess 署名は費用理由で不採用・昇格実行も現段階不採用。elevated foreground 中の配送・release 不能は残留 risk として Supported matrix の行条件に表示して閉じる（実需要が出た時だけ昇格 watchdog の opt-in を再検討。計画 §16 に記録）。**Phase 2 exit 判定材料作成済み（2026-08-16・[docs/phase2-exit-assessment.md](docs/phase2-exit-assessment.md)）**: Exit 5条件のうち成立2（条件3 Supported path 明示・条件5 watchdog）、部分成立3——条件2は **1M report replay（G13/G600）と 1,000 generation race を実装し green**（生成器由来の独立 oracle・stuck 0・wrong release 0）、条件1・4は表示系（Desktop UI）待ち。**hotplug 実装済み（2026-08-16）**: 切断検出（WM_INPUT_DEVICE_CHANGE・RIDEV_DEVNOTIFY）を G13/G600 両 live source に実装、`FastPathPump` が Removal→所有 output 全 release＋新規 down 停止（DEV-008）、Arrival→受理再開（default layer 復帰・切断前状態を持ち越さない）。fake suite（`HotplugTests` 6件——保持中切断の自動 release・幽霊 up 無送出・1,000 回抜挿 cycle で stuck 0/wrong release 0）green・全 focused test green（Input 61・Domain 8・Devices 55・Host 4・architecture 4）。**抜線実測も成立（2026-08-16・G600 実機）**: probe `hotplug-smoke` 3段階全 pass——押下中抜線で物理 up なしの合成 release を実測・再接続後の down/up 正常・wrong release 0・drop 0（`probe-output/hotplug-smoke-20260816-032656-711.json`）。**hotplug は fake・実機とも完全成立**。**sleep 実測も成立（2026-08-16・G600 実機・probe `sleep-smoke`）**: スリープ復帰後も fast path が fault なしで動作継続・stuck 0・wrong release 0・sleep 中の device change なし（`probe-output/sleep-smoke-20260816-034117-199.json`）——**Phase 2 Exit 条件2は完全成立（成立3: 条件2・3・5／部分成立2: 条件1・4=表示系のみ）**。**finite macro（DEV-006 有限 sequence）実装済み・実測成立（2026-08-16 午後）**: output token 文法へ `Tap:` 段（chord 段は `Tap:Key:LCtrl+Key:C`）を追加、sequence binding は down 時に全段の down/up を単一 SendInput call で送出・所有なし（MAP-008 停止境界を構造で満たす。timed/repeat/toggle は MAP-007 R5 のまま対象外）。混在・不正段は profile 適用時に拒否。focused test Input 73件 green、`emitter-smoke` 拡張（WH_KEYBOARD_LL の順序付き観測）で sequence 順序完全一致・残留なしを実測（`probe-output/emitter-smoke-20260816-035259-736.json`）。**Desktop UI 骨格実装済み・実走成立（2026-08-16 午後）**: `OpenLogicool.Desktop` に pure builder（`InputStudioReportBuilder`——catalog 全 control の欠落なし列挙・実測根拠→capability 表示（確認済み→Supported／強い推定→Experimental・Contracts の `ConfirmedButtons` が根拠）・profile 変換表示・G600 制約全文）＋ read-only WPF window、`OpenLogicool.Host ui [--db] [--duration-ms]` で起動（fast path は起動しない）。focused test 6件 green（`tests/OpenLogicool.Desktop.Tests`）・実 DB＋実列挙で起動→描画→終了 exit 0。**Phase 2 Exit 5条件すべて成立（条件1・4も UI 骨格で閉じた・[docs/phase2-exit-assessment.md](docs/phase2-exit-assessment.md)）——Exit 宣言はオーナー裁定待ち**。**foreground app identity（app-first 切替の機能中核）実装済み・実測成立（2026-08-16 午後）**: `AppProfileAssociation`（EXE full path 完全一致・小文字正規化・"*"=既定）＋`IAppAssociationStore`（Persistence migration 003）、Host の `AppProfileResolver`（pure——既定は "*" 指定、profile 1件のみの種別は互換で自動既定、複数で既定なしは明示エラー）＋`ForegroundAppTracker`（GetForegroundWindow→QueryFullProcessImageName・取得不能は既定へ）＋200ms poll thread、`FastPathPump.RequestProfileChange`（任意 thread→worker が RunOnce 冒頭で適用・新規 down から有効・device write なし＝MAP-010）。CLI `associate <profileId> <path|default>`。focused test: Input 75・Host 6・Persistence 11 green。**実測**: 実 G600 wired＋NIKKE 前面で `profile switch: G600 -> 'smoke-notepad'` を確認（temp DB・handled shutdown 正常）。罠: Windows 11 の notepad.exe は Store app へ redirect され System32 path と一致しない——関連付け UI は実行中 process からの path 取得で設計する。**Application list／Workspace 機能中核実装済み（2026-08-16 午後）**: Host に `ApplicationWorkspaceCatalog`（pure——既定 workspace＋app path 単位で両 device の割当をまとめた編集単位。大文字小文字違い path は同一行へ正規化）＋`RunningApplicationCatalog`（可視 top-level window を持つ実行中 app の EXE full path 一覧・正規化 dedupe・自 process 除外——関連付け path はこの一覧を正とする＝Store redirect 罠対応）、CLI `apps [--db]`（workspace 一覧＋実行中 app 一覧・関連付け済みに `[assoc]` 表示）。focused test Host 9件 green。実走: temp DB で import→associate（既定＋explorer.exe）→`apps` が workspace 2行と実行中15 app（NIKKE 本体・launcher・Store 版メモ帳の実 path を含む）を表示、`[assoc]` 一致確認。**オーナー裁定（2026-08-16）: UI は最後、機能を計画順で先行**（binding editor 着手時まで Grok UI 相談も棚上げ）。**Action-centric binding editor 機能中核実装済み（2026-08-16 午後）**: `Contracts/Profiles` に `WorkspaceDocument`（APP-003 の編集単位 wire type——Semantic Action 一覧・device 種別ごとの layer 構成・action→(device, control, layer) binding）、`Profiles` に `WorkspaceCompiler`（pure——MAP-001/002: 一つの action を両 device の control へ compile して ProfileId `{WorkspaceId}-{DeviceKind}` の MappingProfileDocument を生成。構造の誤りは Domain の MappingProfile 検証を通して例外＝適用不可・規則の二重化なし。未割当 action・到達不能 layer は Warnings＝適用前表示＝MAP-004 の中核）、CLI `workspace <json> [--db] [--dry-run]`（compile 結果と警告を表示→保存。保存前に全体の resolver 解決可能性を検証し部分適用状態を作らない）。focused test: Profiles 10件・Host 9件・architecture 4件 green。実走: dry-run で警告2件（未割当 action・到達不能 layer）表示→適用で `smoke-ws-G13`/`smoke-ws-G600` 保存→`apps` で既定 workspace に反映を確認。**workspace revision／undo／export（MAP-009）と適用状態の区別（APP-007）実装済み・実走成立（2026-08-16 午後）**: `Contracts/Profiles` に `IWorkspaceRevisionStore`（append-only）、`Persistence` に migration 004＋`SqliteWorkspaceRevisionStore`（workspace ごと連番・未知 version 拒否）、`Profiles` に `WorkspaceUndo`（pure・無指定は最新の一つ前／番号指定可。append-only のため無指定連続 undo は直前 undo を打ち消す——遡りは番号明示）と `WorkspaceApplyReport`（pure・APP-007 中核——編集/保存/runtime 適用/device 反映を別状態で表示。runtime は常駐 mutex 観測で「未反映（要 host 再起動）」と「非常駐（次回起動時反映）」を区別、保存成功を適用完了と表示しない）。workspace 保存は revision 追記＋profile upsert を単一 SQLite transaction 化（G13 成功/G600 失敗の部分保存を構造的に排除）。CLI `undo <ws> [rev]`／`export <ws> <out.json>`（import は `workspace` command と同一フォーマットで往復）／`revisions <ws>`。focused test: Profiles 18・Persistence 15・Host 9 green。実走: temp DB で dry-run→適用→変更適用→undo 無指定→undo 番号指定→export→再取込の全往復成立（revision 1〜5・段階別報告表示）。**Phase 3 は統括レーン campaign 化（2026-08-16 午後・オーナー指示）**: 工程正本は Lattice plan `phase3-app-first`（11 task・3 phase。`lattice todo status --json` で参照）、campaign 計画正本は [docs/phase3-campaign-plan.md](docs/phase3-campaign-plan.md)（円卓＝実装 sonnet・反証 Grok・親 Fable は裁定/受入のみ）。**p1-core（機能中核 phase）完遂・phase accept 済み（2026-08-16 夕）**: t01〜t07 の 7 task すべて done——APP-004（package identity 照合・migration 005）、APP-005（切替理由の診断化・ProfileSwitchJudge・理由付き log・判断 ring）、APP-008（ForegroundState 3状態・Unknown の明示・誤 Unknown なし）、MAP-004 完全形（衝突全列挙・未確認 control の Experimental 警告）、test field（fast path 非 blocking trace・`run --trace`）、diagnostics・onboarding CLI。実装は implementer（sonnet）委譲＋統括受入の直列円卓なし体制（オーナー裁定: WSL2 浸食禁止→peertable は Windows native 席対応まで保留・提案 chip 化）。**実機実測成立（オーナー手番）**: trace 両実機・explorer path 一致切替・Store メモ帳 package 一致切替・往復の理由付き log 完全・昇格 process で誤 Unknown なし（[evidence/phase3-app-first/p1-core-live-run.md](evidence/phase3-app-first/p1-core-live-run.md)・probe-output/fastpath-live-check-20260816.log）。未実測の持ち越し: NIKKE launcher→本体遷移・Unknown 実トリガー（t11 Exit 判定へ）。**t08 完了（2026-08-16 夕）**: Grok 4.6 の UI 設計を採用（[docs/ui-design-phase3.md](docs/ui-design-phase3.md)・案A「Workspace Command Surface」1画面）。**t09 進行中（第1〜4段まで受入済み）**: 新シェル＋編集対象固定→Action 盤＋Inspector＋保存/undo（Journey B が UI で一巡）→**旧 UI にオーナー0点評価**（内部語彙漏出・ペイン不明・配置誤り）→**モック承認制へ転換**: Grok 制作の画面モック（[docs/ui-mocks/](docs/ui-mocks/)・main/flows/states/index・実機配置は Web 裏取り済み: G600 サイドは横4列×縦3段で手首側下が G9・G13 は 7/7/5/3 段＋LCD 直下に M1〜MR）をオーナー承認→WPF を mock 準拠へ張替え済み（全日本語・禁止語則: token/capability/assoc/revision/compile/identity/Supported/Experimental/既定→共通設定・単一保存 chip・旧台帳撤去）。統括の自己目視も成立（**開発 exe は PowerShell CopyFromScreen で window 直接キャプチャ**——computer-use は未登録 exe を解決できない）。**t09 残作業4点 実装済み（2026-08-16 夜・統括受入済み）**: ①磨き3点（窪みは Border 視覚表現＋ToolTip・ヘッダ overflow はラベル/値分離＋TextTrimming・保存 disabled は Opacity 0.38）②キー録画 modal（`KeyCaptureDialog`/`KeyCaptureTokenizer`——PreviewKeyDown で chord 捕捉→token 変換・マウスは選択肢ボタン・Esc 対応。アプリ選択は既存 `_appPickerPopup` が flows 同等機能と判定し Esc 追加のみ）③診断分離（`DiagnosticsWindow` 新規・表示語彙のみ日本語化・データ層 DEV-005 契約は無変更・footer に入口1つ）④`ui --resident`（fast path＋watchdog 同居・保存直後 `RequestProfileChange` 即時反映・二重起動は明示エラー実測済み）。実装中に発見した既存 crash 欠陥（操作追加直後の SelectionChanged 再入→無限再帰→StackOverflow）も最小修正。focused test: Desktop 58・Host 38・architecture 4 green（統括再走で確認）。**未実測（オーナー手番・t10/t11 で）**: 実機接続下の `--resident` 保存→即時反映、modal の対話操作目視。**t10 完了（2026-08-16 夜・受入済み）**: UI scenario（アプリ選択→操作作成→両 device binding→保存→適用状態表示）を fake（in-memory store）と real（実 SQLite＋実列挙・両実機接続）で同一 public 経路（`IWorkspaceEditorIntents`・共有ロジックは `WorkspaceEditorIntentsSupport` 抽出で構造保証）から実行し **IsMatch=true・不一致0**（`probe-output/ui-test-scenario-20260816-124626-874.json`・fake 側は focused test 9件常設・CLI `ui-test-scenario`）。**t11 材料完成（2026-08-16 夜）**: full regression 314件全 green・cross-provider 監査（Grok 4.6 read-only——Codex は本環境 sandbox 破損で実行不能・罠DB記録済み）重大なし・[docs/phase3-exit-assessment.md](docs/phase3-exit-assessment.md) を Exit 5条件×4値で作成（確認済み3／強い推定2・未確認なし）。**実機手番4点＋持ち越し全て成立（2026-08-17・[evidence/phase3-app-first/t11-live-checks.md](evidence/phase3-app-first/t11-live-checks.md)）**: Alt+Tab 編集対象保持・NIKKE launcher→本体遷移（誤切替なし・path 一致・既定復帰）・**Unknown 実トリガー初実測**（anti-cheat 保護で identity 取得不能→明示＋既定適用）・実機下 `--resident` 保存→即時反映・キー録画 modal 目視。確認中に発見したバグ2件（キー録画 IME 化け 13d35cd・resident 同居時の raw input 登録横取りで実機入力全死 3584440）は根治済み。最終 full regression 全 green。**Phase 3 Exit 成立（2026-08-18 オーナー宣言）**: 全 11 task done・全 3 phase accepted（終端監査は [evidence/phase3-app-first/terminal-audit.md](evidence/phase3-app-first/terminal-audit.md)）・campaign `phase3-app-first` は完全 close。**G600 残置無害化は実装済み・実機確認待ち**（手順は [docs/g600-leftover-operation.md](docs/g600-leftover-operation.md)。常駐中の旧 exe は再起動が要る）。**Phase 4 campaign 起票済み（2026-08-19）**: 計画正本 [docs/phase4-campaign-plan.md](docs/phase4-campaign-plan.md)、工程正本 Lattice plan `phase4-durable-lab`（11 task。t01 Data Flow・t02 Playbook graph は done。t03 journal が in-progress）。実画面は使わない。装飾・実画像は磨きフェーズ（今やらない）。**円卓の入口は peertable room だけ**（Windows native は 2026-08-16 修理後に利用可。Grok 親は HTTP API で着卓）。Grok の `spawn_subagent` は円卓ではない。本端末の監査は Grok（Codex 不可）。

## プロジェクト固有の裁定（要旨）

1. **根拠4値**: すべての成立性は「確認済み／強い推定／未確認／非対応」で表記する。Unverified を Supported と表示しない。実験失敗を別方式へ黙って fallback して成功扱いしない。
2. **fast path 純潔**: Device Input → Mapping Runtime → Input Emitter では AI・network・capture・SQLite・UI rendering を待たない。AI は NextActionProposal を返すだけで、入力・DB・device API へ直接到達しない。
3. **device write 禁止（現段階）**: Phase 0 は read-only。最初の write より前に Migration Safety Gate（完全backup・readback・restore実証）を通す。
4. **製品境界**: DLL注入・memory read/write・anti-cheat回避をしない。Input API成功をゲーム内成功と扱わない。画面文字列・OCR・importデータを信頼された命令として扱わない。
5. **技術基盤**: C# / .NET 10 LTS / WPF / net10.0-windows。Windows UI・HID・capture・input・installer の受入は Windows native 実行だけを証拠とする（WSL2は文書・fixture等のみ）。
6. **未決定を仮実装で埋めない**: 決定期限（計画 §16）までは interface と fixture だけを作り、decision 後に一つの方式を実装する。

## 開発運用

- 通し試験は個別機能の動作確認・原因調査に使わない（最終確認だけ）。focused test で閉じる。
- contract ownership・Lane分割・Definition of Ready/Done は計画 §7 に従う。
- 調査した外部仕様は `rag/` へ、価値ある出力は `docs/` へ還流する。
- **本 repo は通常 commit・push を既定とする**（オーナー裁定 2026-08-15「コミット・プッシュは常に承認する」）。force 系・履歴改変は従来どおり明示指示のみ。
