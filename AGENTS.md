# OpenLogicool — プロジェクト正典

Logicool G13 / G600 を統合する Windows ネイティブアプリ。LGS 9.04.49 の代替となる **Input Studio**（アプリ先行の両デバイス統合設定）と、画面認識付き逐次学習プレイブック基盤 **Game Operator** を、同じ意味操作・アプリプロファイルの上で提供する。

## 正本の所在

- 製品・開発計画の正本: [docs/development-plan.md](docs/development-plan.md)（要件catalog・architecture契約・Phase計画・release gateはこの一冊だけが正）
- 調査知識: [rag/INDEX.md](rag/INDEX.md)（成立性調査・一次資料台帳）
- 障害記録: docs/incidents/
- 本書は計画を複製しない。計画と矛盾したら計画側が正であり、本書を修正する。

## 現在の状態（2026-08-15）

- **Phase 1 完了（Exit 4条件成立）**。判定は [docs/phase1-exit-assessment.md](docs/phase1-exit-assessment.md)。次は Phase 2 だが、**G0-Device-W（実機 write 実験）を他実装より先に単独実施**する順序であり、実機手番とオーナー裁定を要する。
- **G0-Device-RO 通過（オーナー裁定 2026-08-15）**: G13 入力経路・G600 profile read（F6 のみ read 不能を記録）・G600 live input route（全20 control）・Migration Safety Gate 手順定義（[docs/migration-safety-gate.md](docs/migration-safety-gate.md)）・方式 read-only 判定（[docs/g600-route-assessment-2026-08-15.md](docs/g600-route-assessment-2026-08-15.md)）の5条件成立。
- **G0-Automation 通過（オーナー裁定 2026-08-15）**: 実frame連鎖・Data Flow Contract 項目決定・NIKKE 混同なしの3条件成立（[docs/g0-automation-assessment.md](docs/g0-automation-assessment.md)）。
- **G0-Device-W: onboard write 往復成立（2026-08-15 夜・オーナー裁定）**。初回セッションで feature write のずれ（single write・settle なし・handle 再利用が原因）→ マウス操作不能の二次障害を出したが、公開実装調査で「direct SET_FEATURE は正規経路、真因は firmware の settle/handle 敏感性」を解明（[rag/openlogicool/g600-write-protocol-2026-08-15.md](rag/openlogicool/g600-write-protocol-2026-08-15.md)）。evidence-based 作法（fresh open・settle 2s・handle 非再利用・fresh open で verify・一致まで再送）を実装し、**restore（F3/F5 復元）と apply 往復（意図的改変→byte 一致→restore→byte 一致）を実機で成立**（いずれも attempt1 で一致）。詳細は [docs/incidents/2026-08-15-g600-f3-writeback-shift.md](docs/incidents/2026-08-15-g600-f3-writeback-shift.md)。これにより Migration Safety Gate DEV-010 の一巡が実証済み。write 作法は probe の `g600-restore-retry` / `g600-apply-verify` を正とする。F0〜F5 完全 backup は `probe-output/mig01-backup-20260815/` に SHA-256 封入で保持。**0A UI 照合は完了**（[docs/lgs-parity-inventory-2026-08-15.md](docs/lgs-parity-inventory-2026-08-15.md) §5——XML 一次資料判定を維持、UI 限定機能6面と G13 onboard 転送面を記録）。**EXP-G600-03（F0 slot 切替）も成立**（slot 0→1→0 往復・F0 表現モデル実測確定・`probe-output/g600-slot-cycle-20260815-130235-294.json`）。**G0-Device-W は全項目実機実証済み**: restore・apply 往復・slot 切替（方式A成立）に加え、**EXP-G600-02 write 拡張も成立**（方式B変種: G9→F13 書換えで legacy 配送が中間 usage へ変化・raw route 無影響・LGS 巻き戻しなし、[docs/g600-route-assessment-2026-08-15.md](docs/g600-route-assessment-2026-08-15.md) §5）。**G0-Device-W 通過（オーナー裁定 2026-08-15）**: 実証全項目成立＋**route 最終決定「B変種を主経路・Aを補完・C不採用」**（[docs/g600-route-assessment-2026-08-15.md](docs/g600-route-assessment-2026-08-15.md) §5、計画 §16 に記録）。**Phase 2 着手済み**: EXP-IN-03（watchdog 実測）完了——SendInput 合成 key-down は hard kill 後も残留（OS 自動 release なし・2試行再現）、別 process key-up で release 成立。**watchdog 採用必須が確定**（計画 §16。昇格/uiAccess 採否のみ EXP-IN-01 elevated 実測待ち）。probe は `crash-keystate`。**G13 adapter 実装済み（2026-08-15 深夜）**: `Devices.G13` に pure parser（実測台帳 [docs/probes/g13-input-map-2026-08-15.md](docs/probes/g13-input-map-2026-08-15.md) の確認済み／強い推定 bit のみ・未確認 bit と jitter bit は無視）＋ recorded／live adapter（同一 `G13ReportStream` 経路）。実録 fixture（`fixtures/devices/g13-input-report.recheck.v1.json`・243 report）の replay が期待 edge 列12件と一致、live 実機 smoke（`g13-adapter-smoke`）で列挙・単独押下・chord・stick 押込み・stick sample 315件・drop 0 を確認（`probe-output/g13-adapter-smoke-20260815-234242.json`）。**G600 input adapter 実装済み（2026-08-16 未明）**: `Devices.G600` に pure parser（実測台帳 [docs/probes/g600-input-map-2026-08-15.md](docs/probes/g600-input-map-2026-08-15.md)。bit は G 番号と連番一致・byte4 ホイールは「最後の回転差分を latch」を実測で精密化——回転 event はボタン変化なし report だけ）＋ recorded／live adapter（同一 `G600ReportStream` 経路・page 0xFF80）。実録 fixture 2件（`fixtures/devices/g600-input-report.{normal,g9-g20}.v1.json`・全20 control とホイール両方向・latch 罠ケースを網羅）の replay が期待 edge 列48件＋26件・wheel tick 15件と一致（focused test 28件 green）。**live 実機 smoke（`g600-adapter-smoke`）成立（2026-08-16 朝）**: 列挙（実 ContainerId 取得）・サイド押下（G12/G16/G17）・G7/G8・G6(G-Shift)・wheel tick 上9/下12・全 edge 対・drop 0（`probe-output/g600-adapter-smoke-20260816-085722.json`）。**G600 side remap write モジュール実装済み（2026-08-16 朝）**: `Devices.G600` の `G600SideRemap`（pure）が clean F3 から side 12ボタン（G9〜G20）を中間 usage F13〜F24（0x68〜0x73）へ書き換えた payload を構築——**通常層（offset 55〜90）と G-Shift 層（118〜153）の両方**を書き換え（G6 押下中の shifted legacy 配送も無害化対象のため）、他 byte は bytes ごと保持。focused test 16件 green（offset・usage・保持性・非破壊・IsApplied・検証拒否）。probe `g600-side-remap`（clean 前提→apply→fresh verify→restore→fresh verify の往復）が**実機成立**: apply・restore とも attempt1 で byte 一致（`probe-output/g600-side-remap-20260816-000320-991.json`）。残置運用（Input Studio profile 管理下での適用・解除）は Mapping Runtime 側で設計する。**Mapping Runtime 中核実装済み（2026-08-16 朝）**: `OpenLogicool.Input` に `MappingProfile`（pure data＋検証——layer 集合・latch selector＝G13 M1/M2/M3 相当・hold selector＝G600 G-Shift 相当・(control×layer)→output token binding）と `DeviceMappingRuntime`（device instance 単位の pure 状態機。既存 `PressOwnershipState` の上で MAP-003/005/006・DEV-007/008 を実装——down 時に profile/layer/mapping revision/output 集合を固定、up は再解決せず down 時集合だけ解放、layer/profile 変更は新規 down から有効、Stop で新規 down 抑止＋全所有 release）。I/O なし・ホイール tick は binding 対象外（button edge のみ）。focused test 18件 green（`tests/OpenLogicool.Input.Tests`）＋ architecture test 4件 green。**Input Emitter＋watchdog 実装済み・実測成立（2026-08-16 朝）**: `OpenLogicool.Input` に output token 解釈（`Key:<名前>`／`Vk:0xNN`／`Mouse:<button>`・pure・未知 token は拒否）、`SendInputEmitter`（chord は単一 SendInput call・部分成功は `OutputEmitFaultException` で fault 化し自動再送しない）、`WatchdogChannel`＋`GuardedOutputEmitter`（down は watchdog 通知→送出、up は送出→通知の順で「watchdog が知らない down」を構造的に排除）。`OpenLogicool.Watchdog` は依存ゼロの独立 exe（stdin 行 protocol・EOF＝host 死亡で追跡中 output を全 release・EXIT で release なし終了）。probe `emitter-smoke` が実測成立: roundtrip 8 check 全 pass（単一 key・chord・handled stop release・graceful exit）、**hard kill 後 35ms で watchdog release（NFR-008 250ms 予算内）**——DEV-009 の watchdog 条件を製品モジュールで実証（`probe-output/emitter-smoke-20260816-003043-049.json`）。UIPI/elevated 条件（EXP-IN-01）と昇格/uiAccess 採否は未決のまま。focused test: Input 48件 green・architecture 4件 green。**FastPathPump 実装済み（2026-08-16 朝）**: `OpenLogicool.Input` の `FastPathPump` が Device Input→Mapping Runtime→Input Emitter を専用 worker thread で接続（§6.1〜6.2 準拠——非 blocking pull・AI/network/capture/SQLite/UI を待たない・source の drop 検出/未知 device/emitter fault は継続せず fault 停止して全所有 output を release）。focused test 6件（計 Input 54件 green・同期 `RunOnce` ＋ worker thread 実走・fault 時 release 検証）。probe `fastpath-smoke` 実装済み（実機 G13/G600 →F13〜F24 送出・layer 動作・drop/fault/全 emit を JSON 証跡化）。**live 通し smoke 成立（2026-08-16 朝・実機）**: G13/G600 両実機を同時列挙し、45秒間で 30 input 処理・9 押下対（F14/F16/F19/F21〜F24）を down/up 完全対で送出・**drop 0・fault なし**——通常層・G600 G-Shift hold 層・G13 M2 latch 層の全動作を含む（`probe-output/fastpath-smoke-20260816-004442-598.json`）。**fast path（Device Input→Mapping Runtime→Input Emitter＋watchdog）は両実機で end-to-end 実証済み**。Phase 2 の残り実機項目: EXP-IN-01（standard/elevated 受理分類。elevated は UAC 承認要＝オーナー手番）。**Profiles 永続化実装済み（2026-08-16 朝）**: `MappingProfile` を計画 §6.3 どおり Domain へ移設（Input の runtime は Domain 参照のまま無変更）。`Contracts/Profiles` に wire type `MappingProfileDocument`＋port `IMappingProfileStore`、`Profiles` に materializer（document→MappingProfile。検証は Domain 構築子・未知 schema version 拒否）、`Persistence` に migration 002＋`SqliteMappingProfileStore`（versioned JSON 格納・再 open 復元・未知 version は読み飛ばさず fail）。focused test: Profiles 4件・Persistence 8件 green（temp file 再 open の復元実証込み）。**Host resident 化（最小形）実装済み・実機 smoke 成立（2026-08-16 朝）**: `OpenLogicool.Host` が exe 化——`ResidentInputHost`（SQLite open→migrate→profile 復元→実機列挙→fast path＋watchdog 起動→handled shutdown）、`SingleInstanceGuard`（named mutex）、`HostProfileSelection`（pure・DeviceKind ごと 0/1 profile、複数は明示エラー）、CLI `import <json>`／`run [--duration-ms]`。profile が無い種別は配線しない（黙って既定値を作らない）。**実機 smoke 成立**: import 2 profile→再起動復元→G13/G600 両実機 wired 2→resident→handled shutdown exit 0、二重起動は2プロセス実測で拒否（OPS-002 の設定復元と §6.2 の二重起動防止を実機で実証）。focused test: Host 4件 green。tray icon・既存 instance への UI activation 転送は Desktop UI 実装時。**EXP-IN-01 standard 側実測完了（2026-08-16 朝）**: probe `sendinput-accept`（test target window の受信 log と送信列の突合・API 戻り値に依らない判定）で、**standard foreground window への keyboard SendInput 配送は確認済み**——単一 key・chord とも6/6 完全順序一致（`probe-output/sendinput-accept-standard-20260816-014738-596.json`、計画 §16 に記録）。残るオーナー手番: EXP-IN-01 elevated 側（UAC 承認要）と Phase 2 exit 裁定。次の実装候補（裁定待ち）: Desktop UI 骨格（Phase 3 レーン）。

## プロジェクト固有の裁定（要旨）

1. **根拠4値**: すべての成立性は「確認済み／強い推定／未確認／非対応」で表記する。Unverified を Supported と表示しない。実験失敗を別方式へ黙って fallback して成功扱いしない。
2. **fast path 純潔**: Device Input → Mapping Runtime → Input Emitter では AI・network・capture・SQLite・UI rendering を待たない。AI は NextActionProposal を返すだけで、入力・DB・device API へ直接到達しない。
3. **device write 禁止（現段階）**: Phase 0 は read-only。最初の write より前に Migration Safety Gate（完全backup・readback・restore実証）を通す。
4. **製品境界**: DLL注入・memory read/write・anti-cheat回避をしない。Input API成功をゲーム内成功と扱わない。画面文字列・OCR・importデータを信頼された命令として扱わない。
5. **技術基盤**: C# / .NET 10 LTS / WPF / net10.0-windows。Windows UI・HID・capture・input・installer の受入は Windows native 実行だけを証拠とする（WSL2は文書・fixture等のみ）。
6. **未決定を仮実装で埋めない**: 決定期限（計画 §16）までは interface と fixture だけを作り、decision 後に一つの方式を実装する。

## 開発運用

- 通し試験は個別機能の動作確認・原因調査に使わない（最終確認だけ）。focused test で閉じる。
- contract ownership・Lane分割・Definition of Ready/Done は計画 §7 に従う。
- 調査した外部仕様は `rag/` へ、価値ある出力は `docs/` へ還流する。
- **本 repo は通常 commit・push を既定とする**（オーナー裁定 2026-08-15「コミット・プッシュは常に承認する」）。force 系・履歴改変は従来どおり明示指示のみ。
