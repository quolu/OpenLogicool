# Game Operator failure UX（Phase 5 t10）

capture fault、認識不能、絶対座標だけの操作を利用者へ別々に表示する。capture fault は停止理由と再校正を示し、別 backend への自動 fallback を示さない。Ambiguous／Unknown／Unavailable は自動操作条件ではない。Unverified／Unsupported は一つの実 game 成功を一般対応と表示しない。
