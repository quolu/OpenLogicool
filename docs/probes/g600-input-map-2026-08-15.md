# G600 input 経路と report 対応表（Deliverable 0B / 2026-08-15 実測）

- 取得経路: user-mode Raw Input（mouse TLC ＋ usage page 0xFF80 RIDEV_PAGEONLY | RIDEV_INPUTSINK）
- device: `HID\VID_046D&PID_C24A`（firmware release 7702）、LGS 9.04.49 稼働環境下
- 一次データ: `probe-output/rawinput-g600-normal-20260815-135438.jsonl`, `rawinput-g600-gshift-20260815-135756.jsonl`, `rawinput-g600-bottomright-20260815-140840.jsonl`, `rawinput-g600-bottomrow-20260815-141033.jsonl`, `rawinput-g600-mid4-20260815-143319.jsonl`, `rawinput-g600-mid-reverse-20260815-143730.jsonl`, `rawinput-g600-g9-g20-20260815-144504.jsonl`（G 番号順の確定試行）

## 結論: live input route（R-01）

**G600 の全物理入力は、vendor collection (MI_01/COL02) の input report 0x80 として user-mode Raw Input へ届く。確認済み。**
LGS の profile 割当と独立に、1 control = 1 bit の生状態が読める。firmware write なしで全ボタン識別が成立する。

## report 0x80 対応表

| 位置 | control | 根拠 |
|---|---|---|
| byte0 | Report ID 0x80 | 確認済み |
| byte1 bit0 | 左クリック | 確認済み |
| byte1 bit1 | 右クリック | 確認済み |
| byte1 bit2 | ホイール押込み | 確認済み |
| byte1 bit3 | ホイール左チルト | 確認済み |
| byte1 bit4 | ホイール右チルト | 確認済み |
| byte1 bit5 | **G-Shift**（押下中保持。修飾bit方式） | 確認済み |
| byte1 bit6 | ホイール後ろボタン（手前側） | 確認済み |
| byte1 bit7 | ホイール後ろボタン（奥側） | 確認済み |
| byte2 bit0〜bit7 | サイド G9〜G16（bit 番号昇順＝G 番号昇順） | 確認済み |
| byte3 bit0〜bit3 | サイド G17〜G20 | 確認済み |
| byte4 | ホイール回転差分（符号付き、上=+1／下=-1） | 確認済み |
| byte5 | 定数 0x16 を観測。意味未特定 | 未確認 |

## G-Shift 層の識別（受入条件）

G-Shift 押下中もサイド／クリックの bit 位置は不変で、byte1 bit5 が層を示す。**通常／G-Shift の識別は raw route 単独で成立（確認済み）**。

## legacy 重複の分類（実測）

同一押下に対して次の並行配送を観測:

1. **raw report 0x80**（vendor collection）— 全 control、割当非依存
2. **mouse TLC (MI_00)** — クリック・チルト・ホイールを標準 mouse event として配送（チルトは button4/5 扱い）
3. **LGS 仮想キーボード (PID C232)** — サイドボタン等を現行 profile の割当キー（本環境ではテンキー）として配送
4. G600 自身の keyboard TLC (MI_01/COL01) からの配送は**観測されなかった**（LGS filter が吸収していると推定）

## 未解決事項

- **サイド12ボタンの確定と誤判定の訂正**（2026-08-15 最終試行 `rawinput-g600-g9-g20-20260815-144504.jsonl`：印字 G 番号順に G9→G20 を押下）:
  - **12ボタン全数が固有 bit を持ち正常動作（確認済み）**。byte2 bit0〜7 = G9〜G16、byte3 bit0〜3 = G17〜G20 の完全連番。同押下で LGS 仮想キーボードが Kp1, Kp2, …, Kp9, Kp0, Kp−, Kp+ を配送し、これは F4 profile（[g600-profile-decode-2026-08-15.md](g600-profile-decode-2026-08-15.md)）の G9〜G20 割当順と完全一致。
  - **過去の「中段◯番目が未発火／故障」判定はすべて誤りで撤回する**。原因は2つの複合: (1) 空間語（上段・中段・左右）による押下指示が実機の並びの解釈ズレを生んだ (2) 解析スクリプトがセッション最初の raw report を差分初期値に使い、**各セッションの最初の1押下を系統的に取りこぼしていた**（本セッションの G9 押下 t=11.6s が抽出から欠落し、生ログ精査で発見）。故障ボタンは存在せず、全入力一意識別は**全20 control で成立**。
- byte5（0x16 定数）と、HidSharp 列挙に mouse TLC が出ない件（Raw Input では届くため実害なし）の意味づけ。
